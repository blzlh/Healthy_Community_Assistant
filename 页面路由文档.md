# Healthy Community Assistant - 页面路由文档

> 生成时间：2026-04-15  
> 项目：健康社区助手

---

## 📄 页面路由 (Pages)

### 1. 根路由
| 路由路径 | 文件位置 | 页面名称 | 功能描述 |
|---------|---------|---------|---------|
| `/` | `app/page.tsx` | 首页 | 重定向到 `/home` |

---

### 2. 首页模块
| 路由路径 | 文件位置 | 页面名称 | 功能描述 |
|---------|---------|---------|---------|
| `/home` | `app/home/page.tsx` | 主页 | 应用主页，展示功能亮点、快捷操作入口 |

**主要功能：**
- 展示健康档案、智能提醒、社区互助等功能亮点
- 提供在线咨询、智能问答、健康计划的快捷入口
- 响应式设计，支持移动端和桌面端

---

### 3. 认证模块
| 路由路径 | 文件位置 | 页面名称 | 功能描述 |
|---------|---------|---------|---------|
| `/auth/login` | `app/auth/login/page.tsx` | 登录页 | 用户/管理员邮箱验证码登录 |
| `/auth/register` | `app/auth/register/page.tsx` | 注册页 | 新用户邮箱验证码注册 |

**登录页功能：**
- 支持邮箱验证码登录
- 支持普通用户和管理员两种身份登录
- 支持验证码重新发送
- 登录成功后跳转到 `/home`

**注册页功能：**
- 邮箱验证码注册
- 设置用户名和基本信息
- 注册成功后跳转到登录页

---

### 4. 社区模块
| 路由路径 | 文件位置 | 页面名称 | 功能描述 |
|---------|---------|---------|---------|
| `/community` | `app/community/page.tsx` | 社区首页 | 社区动态列表，发帖、浏览、点赞、评论 |
| `/community/new` | `app/community/new/page.tsx` | 发布动态 | 创建新的社区动态 |
| `/community/edit/[id]` | `app/community/edit/[id]/page.tsx` | 编辑动态 | 编辑指定的社区动态 |

**社区首页功能：**
- 展示所有社区动态列表
- 支持发布新动态
- 支持点赞和评论
- 支持编辑和删除自己的动态

---

### 5. 聊天模块
| 路由路径 | 文件位置 | 页面名称 | 功能描述 |
|---------|---------|---------|---------|
| `/chat` | `app/chat/page.tsx` | 聊天室 | 公共聊天室，实时消息交流 |

**聊天室功能：**
- 实时消息发送和接收
- 支持文本消息
- 显示在线用户
- 消息历史记录

---

### 6. 健康分析模块 ⭐
| 路由路径 | 文件位置 | 页面名称 | 功能描述 |
|---------|---------|---------|---------|
| `/healthAnalyzer` | `app/healthAnalyzer/page.tsx` | 健康分析首页 | 选择服务模式（对话/分析） |
| `/healthAnalyzer/chat` | `app/healthAnalyzer/chat/page.tsx` | 自由对话 | 与 AI 进行健康对话 |
| `/healthAnalyzer/analysis` | `app/healthAnalyzer/analysis/page.tsx` | 健康数据分析 | 录入健康数据并获取 AI 分析 |

**健康分析首页功能：**
- 提供两种服务模式选择
- 自由对话：直接与 AI 健康助手对话
- 健康数据分析：录入数据后进行专业分析

**自由对话功能：**
- 与 AI 进行健康相关对话
- 支持对话历史记录
- 支持从分析页面跳转，带健康数据上下文
- 支持流式响应

**健康数据分析功能：**
- 录入血压、心率、血糖、体重、身高、年龄等健康指标
- AI 基于数据生成个性化健康分析报告
- 支持追问和建议问题
- 分析完成后可跳转到对话模式继续咨询

---

### 7. 健康记录模块 ⭐
| 路由路径 | 文件位置 | 页面名称 | 功能描述 |
|---------|---------|---------|---------|
| `/healthRecords` | `app/healthRecords/page.tsx` | 健康记录管理 | 管理个人健康数据记录 |

**健康记录功能：**
- 查看历史健康记录列表
- 新增健康数据记录
- 编辑已有记录
- 删除记录
- 支持按日期筛选
- 数据可视化展示（图表）

---

### 8. 管理员模块
| 路由路径 | 文件位置 | 页面名称 | 功能描述 |
|---------|---------|---------|---------|
| `/admin/users` | `app/admin/users/page.tsx` | 用户管理 | 管理员管理用户账号 |

**用户管理功能：**
- 查看所有用户列表
- 封禁/解封用户账号
- 查看用户基本信息
- 搜索用户

---

## 🔌 API 路由 (API Routes)

### 1. 认证相关 API
| 路由路径 | HTTP 方法 | 文件位置 | 功能描述 |
|---------|----------|---------|---------|
| `/api/auth/login` | POST | `app/api/auth/login/route.ts` | 发送登录验证码 |
| `/api/auth/register` | POST | `app/api/auth/register/route.ts` | 用户注册 |
| `/api/auth/me` | GET | `app/api/auth/me/route.ts` | 获取当前用户信息 |

---

### 2. 社区相关 API
| 路由路径 | HTTP 方法 | 文件位置 | 功能描述 |
|---------|----------|---------|---------|
| `/api/community/posts` | GET, POST | `app/api/community/posts/route.ts` | 获取动态列表 / 创建动态 |
| `/api/community/posts/[id]` | GET, PUT, DELETE | `app/api/community/posts/[id]/route.ts` | 获取/更新/删除指定动态 |
| `/api/community/posts/[id]/like` | POST | `app/api/community/posts/[id]/like/route.ts` | 点赞/取消点赞 |
| `/api/community/posts/[id]/comments` | POST | `app/api/community/posts/[id]/comments/route.ts` | 评论动态 |

---

### 3. 健康对话相关 API ⭐
| 路由路径 | HTTP 方法 | 文件位置 | 功能描述 |
|---------|----------|---------|---------|
| `/api/health-conversations` | GET, POST | `app/api/health-conversations/route.ts` | 获取对话列表 / 创建新对话 |
| `/api/health-conversations/[id]` | GET, DELETE | `app/api/health-conversations/[id]/route.ts` | 获取/删除对话详情 |
| `/api/health-conversations/[id]/title` | PATCH | `app/api/health-conversations/[id]/title/route.ts` | 更新对话标题 |
| `/api/health-conversations/records` | GET, POST | `app/api/health-conversations/records/route.ts` | 获取/保存健康记录 |

---

### 4. 健康记录相关 API ⭐
| 路由路径 | HTTP 方法 | 文件位置 | 功能描述 |
|---------|----------|---------|---------|
| `/api/health-records` | GET, POST | `app/api/health-records/route.ts` | 获取记录列表 / 创建记录 |
| `/api/health-records/[id]` | GET, PATCH, DELETE | `app/api/health-records/[id]/route.ts` | 获取/更新/删除记录 |

---

### 5. 用户资料相关 API
| 路由路径 | HTTP 方法 | 文件位置 | 功能描述 |
|---------|----------|---------|---------|
| `/api/profile` | GET, PUT | `app/api/profile/route.ts` | 获取/更新用户资料 |
| `/api/profile/admin/users` | GET | `app/api/profile/admin/users/route.ts` | 获取用户列表（管理员） |
| `/api/profile/admin/ban` | POST | `app/api/profile/admin/ban/route.ts` | 封禁/解封用户（管理员） |

---

## 🎨 页面层级结构

```
/ (根路由)
├── home                    # 主页
├── auth
│   ├── login              # 登录
│   └── register           # 注册
├── community
│   ├── (page)             # 社区首页
│   ├── new                # 发布动态
│   └── edit/[id]          # 编辑动态
├── chat                   # 公共聊天室
├── healthAnalyzer
│   ├── (page)             # 模式选择首页
│   ├── chat               # 自由对话模式
│   └── analysis           # 数据分析模式
├── healthRecords          # 健康记录管理
└── admin
    └── users              # 用户管理（管理员）
```

---

## 🔐 权限说明

| 页面类型 | 访问权限 | 说明 |
|---------|---------|------|
| 公开页面 | 所有人 | `/auth/login`, `/auth/register` |
| 用户页面 | 已登录用户 | `/home`, `/community`, `/chat`, `/healthAnalyzer`, `/healthRecords` |
| 管理员页面 | 管理员 | `/admin/users` |

---

## 📊 技术栈

- **框架**: Next.js 14 (App Router)
- **路由**: 基于文件系统的路由
- **认证**: Supabase Auth
- **状态管理**: Zustand
- **UI 组件**: shadcn/ui + Ant Design
- **样式**: TailwindCSS

---

## 🚀 快速导航

| 功能模块 | 路由 | 快速访问 |
|---------|------|---------|
| 首页 | `/home` | 应用入口 |
| 登录 | `/auth/login` | 用户认证 |
| 社区 | `/community` | 社区互动 |
| 聊天 | `/chat` | 实时聊天 |
| AI 对话 | `/healthAnalyzer/chat` | 智能问答 |
| 健康分析 | `/healthAnalyzer/analysis` | 数据分析 |
| 健康记录 | `/healthRecords` | 记录管理 |

---

## 📝 备注

1. **动态路由**: `/community/edit/[id]` 中的 `[id]` 是动态参数，代表动态 ID
2. **API 路由**: 所有 API 路由位于 `/api` 目录下
3. **认证保护**: 大部分页面需要用户登录后才能访问
4. **响应式设计**: 所有页面支持移动端和桌面端访问

---

*文档由 CatPaw 自动生成*
